apiVersion: v1
kind: Service
metadata:
  name: {{ labels.app }}
  namespace: {{ namespace.name }}
spec:
  ports:
  - name: rsync
    protocol: TCP
    port: {{ dataAccess.rsyncPort }}
    targetPort: {{ dataAccess.rsyncPort }}
  selector:
    app: {{ labels.app }}
  type: ClusterIP
